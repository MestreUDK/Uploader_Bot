services:
  - type: web
    name: uploader-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false  # você vai adicionar isso manualmente no painel do Render